/**
 * auth-check.js
 * Auth logic neutralized as per user request to remove sign-in requirements.
 * Keeping file to prevent 404s if referenced elsewhere.
 */
console.log("Authentication checks disabled.");
